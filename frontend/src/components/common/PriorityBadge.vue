<script setup>
defineProps({ priority: String })

const map = {
  low:      'bg-gray-100 text-gray-600',
  medium:   'bg-blue-100 text-blue-700',
  high:     'bg-orange-100 text-orange-700',
  critical: 'bg-red-100 text-red-700',
}
</script>

<template>
  <span :class="['inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium capitalize', map[priority] || 'bg-gray-100 text-gray-600']">
    {{ priority }}
  </span>
</template>
